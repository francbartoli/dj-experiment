============
Installation
============

At the command line::

    $ easy_install dj-experiment

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-experiment
    $ pip install dj-experiment
